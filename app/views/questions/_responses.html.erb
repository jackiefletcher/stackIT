<i class='col-md-1 glyphicon glyphicon-ok'> Best</i>
  <%= render question.responses.first, question: question %>

<% question.responses.voted.each do |response| %>
  <%= render response, question: question %>
<% end %>
